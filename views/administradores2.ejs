<%- include('partials/header') %>

<div class="flex flex-col text-blue">
  <%- include('partials/menu') %> <%- include('partials/nav') %>
  <main class="relative mt-20 ml-0 md:mt-16 md:ml-36 lg:ml-72">
    <div class="min-h-screen p-6">
      <div class="caja">
        <div class="flex flex-col mb-8">
          <form class="space-y-6" action="/administradores" method="get">
            <div class="grid grid-cols-1 gap-4 md:grid-cols-2">
              <div>
                <label
                  for="username"
                  class="block mb-1 text-sm font-bold text-gray-700"
                  >Nombre de usuario</label
                >
                <select class="custom-select">

                  <option selected disabled>Seleccione el nombre</option>
                  <!-- Add more options here -->
                </select>
              </div>
              <div>
                <label
                  for="document"
                  class="block mb-1 text-sm font-bold text-gray-700"
                  >Asignar un documento</label
                >
                <select class="custom-select">

                  <option selected disabled>
                    Seleccione uno o más documentos
                  </option>
                  <!-- Add more options here -->
                </select>
              </div>
            </div>
            <div class="flex justify-start mt-4 space-x-4">
              <button type="submit" class="buttonmd" onclick="window.location.href='/administradores'">GUARDAR USUARIO</button>
              <button type="button" class="button-tertiarymd">CANCELAR</button>
            </div>
          </form>
        </div>
      </div>
      <!-- fin caja-->
    </div>
  </main>
</div>

<%- include('partials/footer') %>
